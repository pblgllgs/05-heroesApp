import { PipeImagenPipe } from './pipe-imagen.pipe';

describe('PipeImagenPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeImagenPipe();
    expect(pipe).toBeTruthy();
  });
});
